# 问题 1 / 问题 2 结果整理（论文可直接改写使用）

## 问题 1：定位区域的直径算法与覆盖性判定

### 1.1 模型

设检测点 $S_i=(x_i,y_i)$ 处测得的示向度为 $\theta_i$（度，正东为 0°，逆时针为正）。
受 $\pm 1^\circ$ 测向误差影响，干扰源必位于以 $S_i$ 为顶点、方向 $\theta_i$、
半张角 $1^\circ$ 的楔形 $W_i=\{P:\angle(P-S_i,\theta_i)\le 1^\circ\}$ 内。
故交会定位区域为

$$R=\Big(\bigcap_{i=1}^{m} W_i\Big)\cap D_{\text{arena}}$$

其中 $D_{\text{arena}}$ 为目标圆域（需要时）。$R$ 是凸多边形（楔形与圆盘均为凸集，
凸集的交仍为凸集），顶点由各楔形边界的两两交点中落入所有楔形的那些构成。

### 1.2 直径算法（步骤 + 复杂度）

1. 以圆域（或足够大的外包正方形）为初始凸多边形；
2. 对每个示向度，用两条边界直线（$\theta_i\pm1^\circ$）做半平面裁剪
   （Sutherland–Hodgman，$O(n)$）；
3. 得到顶点序列后，用**旋转卡壳**在凸多边形上求直径（对踵点对），$O(n)$；
   顶点数很少时也可直接 $O(n^2)$ 枚举顶点对（直径必在顶点处取得）。

整体复杂度 $O(mn)$（$m$ 为检测点数，$n$ 为多边形顶点数）。代码：
`geometry.clip_wedge` + `q12_analysis.polygon_diameter`。

### 1.3 "以定位区域直径为直径的圆能否覆盖该区域"

**结论：不能保证覆盖。**

* 记定位区域直径为 $D$、最小包围圆半径为 $R$。任何集合都有 $R\ge D/2$，
  由 Jung 定理（平面情形）又有 $R\le D/\sqrt{3}$。以 $D$ 为直径的圆半径恰为 $D/2$，
  因此只有当最小包围圆恰好由直径端点确定（$R=D/2$）时才一定覆盖；一般情形需要把
  半径放大到 $D/\sqrt{3}$，即直径放大 $2/\sqrt{3}\approx1.1547$ 倍。
* 反例（数值搜索得到，代码 `q12_analysis.scan_counterexamples`，20 万次抽样命中 1702 例）：
  最严重的一例中，定位区域顶点到直径圆心的最远距离为圆的半径的 **1.0344 倍**，
  即该顶点落在直径圆之外，圆无法覆盖定位区域。
* 对 2、3、4 个检测点各做 4 万次随机抽样，比值上界一致落在 **1.035** 附近，
  说明该比值受"两条楔形边界夹角不超过 $2^\circ$"这一几何结构限制，不会接近
  理论最坏的 $1.1547$。

**工程含义**：不能用"直径圆"作为定位区域的保守界，应当直接计算最小包围圆
（`geometry.minimal_enclosing_circle`）。问题 3/4 的清除判据正是基于最小包围圆半径
$\le 20$ m（清除半径），而不是基于直径。

## 问题 2：第二个检测点的选择策略

### 2.1 建模

设第一个检测点为 $S_1$（取原点），测得示向度指向正北（由旋转不变性可固定方向）。
干扰源 $G$ 在该射线上，距离 $r$ 未知。候选第二检测点
$S_2=d(\cos\alpha,\sin\alpha)$（$\alpha$ 为相对第一示向度方向的偏角）。

对给定的 $r$，两个楔形的交是定位区域，其直径 $D(r)$ 越小说明定位越准。
由于 $r$ 未知，采用**最坏情况准则**：

$$\min_{d,\alpha}\ \max_{r\in[300,1500]\text{m}} D(r)$$

### 2.2 结果

| 情形 | 最优 $d$ | 最优 $\alpha$ | 最坏情况直径 |
| --- | --- | --- | --- |
| 无附加约束 | 1600 m | $\pm40^\circ$ | 75.1 m |
| 要求第二点在最坏情况下仍能收到信号（到源 $\le1000$ m，即最小有效接收半径） | 1200 m | $\pm25^\circ$ | 89.1 m |

对应几何解释：两条示向线尽量接近正交（约 $90^\circ$），且第二点到 S1 的距离与
源距离同量级，同时不能远到脱离最小有效接收半径。

### 2.3 候选区域

取"最坏情况直径不超过最优值 1.5 倍"的集合作为候选区域（代码 `q2_analysis.py`
输出，图 `fig_q2_candidate.png` 中用白色等值线标出）：

* 无约束：$d\in[1100,2900]$ m，$|\alpha|\le65^\circ$；
* 带接收距离约束：$d\in[1000,1200]$ m，$|\alpha|\le40^\circ$。

工程表述：**以第一个检测点为圆心、沿第一条示向线两侧各约 $25^\circ\sim40^\circ$、
距离约 $1.0\sim1.2$ km 的扇形环带**即为第二检测点的候选区域。
