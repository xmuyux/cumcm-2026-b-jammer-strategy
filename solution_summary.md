# 2026 高教社杯 B 题 解答汇总（论文骨架）

> 说明：本文档把四个问题的模型、算法、结论、验证数据集中在一处，可直接改写为论文正文。
> 代码与数据在 `work/b/` 目录。

## 0. 符号与约定

| 符号 | 含义 |
| --- | --- |
| $S_i=(x_i,y_i)$ | 第 $i$ 个检测点坐标（米，正东为 $x$ 轴，正北为 $y$ 轴） |
| $\theta_i$ | 在 $S_i$ 处测得的示向度（度，$[0,360)$，正东为 0°，逆时针为正） |
| $\varepsilon=1^\circ$ | 示向度最大误差 |
| $W_i$ | 由 $S_i,\theta_i$ 确定的楔形区域（半张角 $\varepsilon$） |
| $R=\bigcap_i W_i$ | 交会定位区域（凸多边形） |
| $D$ | 定位区域直径 $\max_{P,Q\in R}\|P-Q\|$ |
| $C_{\min},r_{\min}$ | $R$ 的最小包围圆及其半径 |
| $v=5$ m/s | 机器狗移动速度 |
| $\rho_{\text{eff}}$ | 干扰源最大可被有效接收距离（1000–1500 m） |
| $R_c=20$ m | 光学精确定位与清除半径 |

---

## 问题 1：定位区域直径算法与覆盖性判定

### 模型

测向误差有界，故干扰源必在以 $S_i$ 为顶点、方向 $\theta_i$、半张角 $\varepsilon$ 的楔形内：

$$W_i=\Big\{P:\ \big|\angle(P-S_i,\ \theta_i)\big|\le\varepsilon\Big\},\qquad
R=\Big(\bigcap_{i=1}^{m} W_i\Big)\cap D_{\text{arena}}$$

楔形与圆盘都是凸集，凸集之交仍为凸集，**故 $R$ 是凸多边形**，其顶点由各边界直线的两两交点中
落在所有楔形内的那些构成（最多 $2m$ 条直线，顶点数 $O(m)$）。

### 算法（直径）

1. 以赛场圆域（或足够大的外包矩形）为初始凸多边形；
2. 逐个示向度做半平面裁剪：对 $\theta_i\pm\varepsilon$ 两条边界直线用 Sutherland–Hodgman
   裁剪，复杂度 $O(n)$；
3. 对所得凸多边形求直径：用旋转卡壳枚举对踵点对，$O(n)$；顶点很少时也可 $O(n^2)$ 枚举顶点对
   （凸多边形的直径必在顶点处取得）。

总复杂度 $O(mn)$。实现：`geometry.clip_wedge` + `q12_analysis.polygon_diameter`。

### 结论：以定位区域直径为直径的圆**不能保证**覆盖该区域

设 $R$ 的直径为 $D$、最小包围圆半径为 $r_{\min}$，则恒有

$$\frac{D}{2}\ \le\ r_{\min}\ \le\ \frac{D}{\sqrt{3}}\qquad(\text{Jung 定理})$$

以 $D$ 为直径的圆半径恰为 $D/2$，因此只有 $r_{\min}=D/2$（最小包围圆恰好由直径端点决定）时
才必定覆盖；一般情况下需要把半径放大到 $D/\sqrt3$，即**直径放大 $2/\sqrt3\approx1.1547$ 倍**。

**反例**（数值搜索，20 万次抽样命中 1702 例）：最严重实例中，定位区域某顶点到直径圆圆心的
距离为半径的 **1.0344 倍**，落在圆外。对 $m=2,3,4$ 个检测点各 4 万次随机抽样，比值上界均
稳定在 **1.035** 附近，说明该比值受"两楔形边界夹角不超过 $2\varepsilon$"的几何结构限制，
远小于理论最坏值 1.1547。

**工程含义**：不能用"直径圆"作为定位区域的保守界，必须直接计算最小包围圆。问题 3、4 的清除
判据正是 $r_{\min}\le R_c$。

---

## 问题 2：第二个检测点的选择策略

### 模型

取 $S_1$ 为原点、其示向度方向为正北（旋转不变性）。干扰源 $G$ 位于该射线上，距离 $r$ 未知。
候选第二检测点 $S_2=d(\cos\alpha,\sin\alpha)$，$\alpha$ 为相对第一条示向度的偏角。
两楔形之交即定位区域，其直径 $D(r)$ 表征定位精度。由于 $r$ 未知，采用**极小极大准则**：

$$\min_{d,\alpha}\ \max_{r\in[300,1500]\ \text{m}} D(r)$$

### 结果

| 情形 | 最优 $d$ | 最优 $\alpha$ | 最坏情况直径 |
| --- | --- | --- | --- |
| 无附加约束 | 1600 m | $\pm40^\circ$ | 75.1 m |
| 附加"第二点在最坏情况下仍能收到信号"（到源 $\le1000$ m） | 1200 m | $\pm25^\circ$ | 89.1 m |

几何解释：两条示向线应尽量接近正交（约 $90^\circ$），同时第二点不能远到脱离最小有效接收半径。

### 第二检测点候选区域

取"最坏情况直径不超过最优值 1.5 倍"的点集：

* 无约束：$d\in[1100,2900]$ m，$|\alpha|\le65^\circ$；
* 带接收距离约束：$d\in[1000,1200]$ m，$|\alpha|\le40^\circ$。

工程表述：**以 $S_1$ 为中心、沿第一条示向线两侧各 $25^\circ\sim40^\circ$、距离 $1.0\sim1.2$ km
的扇形环带**。图：`fig_q2_candidate.png`。

---

## 问题 3：全向干扰源的自动搜索定位与清除

### 模型

**(1) 定位子模型**：与问题 1 相同，$k$ 次观测后得到凸定位区域 $R_c$，取其最小包围圆
$(C_{\min},r_{\min})$。

**(2) 清除判据（可证）**：若 $r_{\min}\le R_c=20$ m，则 $R$ 中任意点（含真实源）到 $C_{\min}$
的距离都不超过 20 m，站在 $C_{\min}$ 发 `/clear` **必定命中**。实现中取 17 m 留余量。

**(3) 搜索子模型（覆盖问题）**：全向源在距离 $\le\rho_{\text{eff}}$ 处必被听到，而
$\rho_{\text{eff}}\ge1000$ m。因此若检测点集合 $\mathcal P$ 满足

$$\forall Q\in D_{\text{arena}},\quad \min_{P\in\mathcal P}\|Q-P\|\le 1000\ \text{m}$$

则所有未清除的干扰源必定被扫到。取 $|\mathcal P|=7$（半径 1200 m 的正六边形 + 圆心），
数值验证覆盖半径 **967 m < 1000 m**。

**(4) 调度**：把"检测—定位—清除"组织成一次巡回，用最近邻 + 顺路（绕行 $\le$ 阈值）策略，
并剔除冗余动作：已清除频道不再检测、对某频道已不可能收到信号的格点直接跳过
（若点 $P$ 到定位区域距离 $>1500$ m，则 $P$ 处必然无信号）。

### 结果（真机演练 9 局，全部全清）

清除率 107/107 = 100%，虚拟总时间 4353 ± 255 s，平均定位清除时间 370.7 ± 51.4 s（最差 448 s），
程序运行时间 1.3–1.9 s，每局约 125 次检测。明细见 `practice_runs.csv`。

---

## 问题 4：含定向干扰源的自动搜索定位与清除

### 模型

**(1) 定向源的检测条件**：源位于 $Q$、朝向 $d$，仅在 $|P-Q|\le\rho_{\text{eff}}$ 且
$(P-Q)\cdot d\ge0$ 时可被听到。因 $d$ 未知，要保证检测，检测点集合必须满足

$$\forall Q\in D_{\text{arena}},\quad Q\in \operatorname{conv}\Big\{P\in\mathcal P:\ \|P-Q\|\le \rho_{\text{eff}}^{\min}=1000\ \text{m}\Big\}$$

等价判据：从 $Q$ 看，落入 1000 m 内的检测点**最大角间隙 $<180^\circ$**（代码 `surround.py`）。

**(2) 搜索图案设计**：以"内层三角格点 + 外层圆环"为参数族，用 12–45 m 多套网格交叉验证角间隙
（做网格无关性检验），以巡回路径最短为目标搜索参数，得到

> 内层间距 900 m 的三角格点（$r\le1800$ m）+ 半径 2050 m、12 点、相位 $15^\circ$ 的外环，
> 共 31 个检测点，最大角间隙 **142.5°**（$<180^\circ$，留 37.5° 余量），巡回长度 **4156 s**。

早期图案为 31 点六方格网（巡回 5400 s），优化后路径缩短 23%，实测平均定位清除时间由
941 s 降至 646 s。粗网格会把角间隙低估近 20°，故必须用细网格验证——这是图案筛选的关键。

**(3) 定位子模型**：与问题 3 相同，但定向源可能在某些测点处无信号，故只用
"有示向度"的观测构成约束，并用**加权最小二乘交会**（权重 $1/d_i$）估计源位置；
当所有示向度近乎平行（无法定距）时，改为**侧向探测**（垂直于示向度方向偏移布点）。

**(4) 兜底**：若定位区域仍偏大，围绕估计位置按环状点阵依次 `/clear`（每次失败仅耗 3 s）。

### 结果（真机演练 9 局，含 16 个全定向源的极端案例）

清除率 100%，虚拟总时间 8237 ± 540 s，平均定位清除时间 691.5 ± 130 s（最差 843 s），
程序运行时间 2.5–5.5 s，每局 300–480 次检测。

**对照实验**：同一套参数下，若问题 4 误用问题 3 的 7 点覆盖网（无"包围"保证），
16 个全定向源的案例只能清除 14 个（87.5%）——反向印证了包围条件的必要性。

---

## 结果汇总

| 问题 | 核心结论 | 验证状态 |
| --- | --- | --- |
| 1 | 定位区域=楔形交集（凸多边形），直径 $O(mn)$ 可求；**直径圆不能保证覆盖**，最坏需放大 $2/\sqrt3$ 倍，实测反例比 1.0344 | 已完成，含反例与图 |
| 2 | 极小极大准则下最优第二检测点：$d\approx1.0\sim1.2$ km、偏角 $\pm25^\circ\sim40^\circ$（带接收约束） | 已完成，含等值线图 |
| 3 | 7 点覆盖网（覆盖半径 967 m）+ 最小包围圆清除判据 | 真机演练 9/9 全清 |
| 4 | 31 点包围式图案（最大角间隙 142.5°）+ 加权最小二乘交会 + 侧向探测 | 真机演练 9/9 全清 |

## 尚未完成

1. **6 次正式测试**（问题 3、问题 4 各 3 次）及其日志导出——论文表 1 与支撑材料需要；
2. **论文正文排版**（摘要、问题重述、假设、符号、模型与求解、结果分析、检验、评价、附录代码）；
3. **支撑材料打包**（源码、日志、演练统计）。
